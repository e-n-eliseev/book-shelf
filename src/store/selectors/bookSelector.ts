import type { RootState } from "../store";

// export const getBooks = (state: RootState) => state.getBook.books;
// export const getSliderBooks = (state: RootState) => state.getBook.sliderBooks;
// export const getTotalQuantity = (state: RootState) =>
//   state.getBook.totalBookQuantity;
// export const getSearchName = (state: RootState) => state.getBook.searchParam;
// export const getCurrentBook = (state: RootState) => state.getBook.currentBook;
// export const getFeedBaks = (state: RootState) => state.getBook.feedBack;
